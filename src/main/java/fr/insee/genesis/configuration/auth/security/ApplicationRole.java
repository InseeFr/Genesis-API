package fr.insee.genesis.configuration.auth.security;

public enum ApplicationRole {
    ADMIN,
    USER_KRAFTWERK,
    USER_PLATINE,
    COLLECT_PLATFORM,
    SCHEDULER,
    READER,
    USER_BACK_OFFICE
}

